/**
 * Placeholder image URLs used when Custom API returns bytes or base64 data.
 * These static assets are used instead of storing image bytes in the backend.
 */
export const PLACEHOLDER_IMAGE_URL = '/assets/generated/app-logo.dim_512x512.png';
